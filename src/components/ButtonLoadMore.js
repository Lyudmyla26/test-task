import { Button } from "./CarItem.styled";

const ButtonLoadnMore = ({onClick}) => {
    return (
        <>
            <Button type="button" onClick={onClick}>Load more</Button></>
    )
};
export default ButtonLoadnMore;